# TODO

- [x] Add relations between messages and users via foreign keys in user table

- [x] Add validation for models
      (partially)

- Add logout endpoint
- Create return types for services and handlers
- Move all middleware in separate file
